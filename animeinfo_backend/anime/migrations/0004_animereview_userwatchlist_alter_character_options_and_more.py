from django.db import migrations

class Migration(migrations.Migration):
    # Minimal stub to satisfy existing migration dependencies.
    dependencies = [
        # Intentionally left empty so this becomes a concrete node other migrations can depend on.
        # If you know the correct previous migration (e.g. '0003_...'), you can replace [] with:
        # ('anime', '0003_<name_here>')
    ]
    operations = [
        # No database operations here — this is a no-op stub to repair the migration graph.
    ]
